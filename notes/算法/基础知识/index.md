# 基础知识

1. 不使用额外变量，交换两个变量值（a, b需要是两个不同的地址空间变量）

```python
a = a ^ b
b = a ^ b
a = a ^ b
```

1. 对某个数而言，如果需要寻找其作为二进制的最右侧的“1”，只需要对其进行如下操作：

```python
re = x & (~x + 1) # x 和 x 的取反加一 进行“与”操作
```

1. 二分法的归并执行过程类似于一颗二叉树



